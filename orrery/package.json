{
  "name": "orrery",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "3D earth-clock — interactive globe with weather data and (eventually) the full solar system",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
